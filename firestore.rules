rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Rules are wide open for this demo.
    // In a real application, you would secure these endpoints.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
